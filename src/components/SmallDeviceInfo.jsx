import { Box, Text } from "@chakra-ui/layout";

export const SmallDeviceInfo = () => {
  return (
    <Box className="sm-screen">
      <Text>Sorry, these App is only available for desktop screens</Text>
    </Box>
  );
};
